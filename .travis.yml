language: python
python:
  - "3.7"
# command to run tests
before_install:
  - sudo apt-get -y install libgmp3-dev libgmp-dev libmpfr-dev libmpc-dev

install:
  - "pip install -e ."
script:
  - pytest


